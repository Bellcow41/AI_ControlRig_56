# ===================================
# Bone Mapping AI - Dependencies
# ===================================

# === 기본 도구 ===
numpy>=1.24.0
pandas>=2.0.0
tqdm>=4.65.0
pyyaml>=6.0

# === 데이터 처리 ===
datasets>=2.14.0
transformers>=4.36.0
tokenizers>=0.15.0

# === 파인튜닝 (Unsloth - 빠른 학습) ===
# Unsloth는 별도 설치 필요 (아래 참조)
# pip install "unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git"

# === PyTorch (CUDA 버전에 맞게 설치) ===
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
torch>=2.1.0
accelerate>=0.25.0
bitsandbytes>=0.41.0

# === PEFT (LoRA) ===
peft>=0.7.0

# === 평가 & 유틸 ===
scikit-learn>=1.3.0
matplotlib>=3.7.0
seaborn>=0.12.0

# === API 서버 (선택사항) ===
fastapi>=0.104.0
uvicorn>=0.24.0

# === 언리얼 연동 ===
# unreal은 언리얼 에디터 내장 Python에서만 사용 가능















